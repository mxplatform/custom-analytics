version: 2

sources:
  - name: clickhouse
    database: anx_stg1_mercury
    tables:
      - name: msg_totals_bysenddate_v1
        +enabled: "{{ target.name == 'dev' }}"

  - name: ds_internal
    database: ds_internal
    tables:
      - name: customer_metadata1
        +enabled: "{{ target.name == 'dev' }}"